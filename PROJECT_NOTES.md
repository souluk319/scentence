# Project Notes

이 파일은 나중에 다시 작업할 때 맥락을 빠르게 복구하기 위한 내부용 메모입니다.

## 현재 요약

- 프로젝트명: SCENTENCE
- 스택: Next.js 16, React 19, TypeScript, Tailwind CSS 4, Framer Motion, NextAuth
- 목표: 랜딩 페이지, 추천 흐름, 아카이브, 레이어링, 네트워크, 위키형 경험을 하나의 향수 서비스로 묶기

## 최근 정리 내용

- 지저분했던 기존 Git 히스토리를 유지하지 않고, 현재 로컬 프로젝트 상태 기준으로 다시 정리하는 방향으로 진행
- `.gitignore`에서 아래 항목이 제외되도록 확인함
  - `.env`
  - `.env.local`
  - `.next/`
  - `node_modules/`
  - `.claude/`
  - `.DS_Store`
- 기본 템플릿 README를 프로젝트용 문서로 교체함

## 커서 애니메이션 메모

- 메인 커서 이펙트 컴포넌트: `components/common/HeroLiquidCursor.tsx`
- `app/page.tsx`에서 마운트됨
- 현재 방향:
  - 메인 랜딩 페이지 전체 기준으로 적용
  - 효과 구조 자체는 크게 흔들지 않기
  - 움직임보다 색을 조심스럽게 조정하는 쪽으로 접근
- 최근 색상 조정:
  - 보라기 많은 핑크에서 조금 정돈된 톤으로 수정
  - 이후 채도를 살짝 낮춰서 더 차분하게 조정
- 주의:
  - 이 영역에서 이미 시간이 많이 소모됨
  - 다음 수정은 작은 단위로 해야 함
  - 범위, 질감, 알파, 블러, 색을 한 번에 바꾸지 말 것

## 현재 걸리는 점

- 커서 애니메이션의 미감은 아직 완전히 만족스럽지 않음
- Codex 앱의 diff / undo 워크플로우는 나중에 다시 확인할 필요가 있음
- 이전에는 생성물 / 로컬 폴더가 Git 히스토리에 섞여서 repo 위생 상태가 좋지 않았음

## 다음에 다시 작업할 때 체크할 것

- 먼저 `README.md`보다 이 파일을 읽기
- 랜딩 페이지를 건드리기 전 `app/page.tsx`와 `components/common/HeroLiquidCursor.tsx`를 같이 확인하기
- 실행 기본 순서:

```bash
npm install
npm run dev
```

## 다음 추천 작업

- 정리된 로컬 기준으로 GitHub 메인 푸시 마무리
- 푸시 후 배포 상태 확인
- 랜딩 페이지 커서 이펙트를 좁은 목표로 다시 조정
- README에 배포 URL, 스크린샷, 환경 변수 설명 추가
- 라우트별 기능 책임과 불필요 파일 한 번 더 점검

## 작업 기록

### 2026-03-12

- 문서 구조를 외부용 / 내부용으로 분리함
- `README.md`를 프로젝트 소개 문서로 교체함
- `PROJECT_NOTES.md`를 작업 복구용 메모로 추가함
- 커서 애니메이션 관련 맥락을 기록해 둠
